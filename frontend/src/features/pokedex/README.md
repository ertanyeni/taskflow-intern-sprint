# features/pokedex/ — Stajyer 1 modülü (AÇIK)

Bu klasör bilerek boş. Stajyer 1 burayı **S1-3 (rung-3)** görevinde doldurur.

Aynalanacak gold referans: `frontend/src/features/meals/`
- `types.ts`, `mealKeys.ts`, `api/mealsClient.ts`, `hooks/*`, `components/*`

Veri kaynağı: PokeAPI — `https://pokeapi.co/api/v2/pokemon` (anahtarsız).

Beklenen dosyalar (rung ilerledikçe):
- `types.ts`, `pokedexKeys.ts`, `api/pokedexClient.ts`
- `hooks/usePokemonList.ts`, `hooks/usePokemon.ts`, `hooks/useAiSummary.ts`
- `components/PokemonList.tsx` (+ test), `PokemonCard.tsx`, `TypeFilter.tsx`, `AiSummaryPanel.tsx`

Detay: `docs/briefs/pokedex.md`.
